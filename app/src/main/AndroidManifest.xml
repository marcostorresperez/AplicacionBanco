<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="matope.simarro.pmdm_t3p9_torres_marcos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".otros.Audio"
            android:enabled="true"/>

        <activity android:name=".principal.PreferenciasActivity"></activity>
        <activity android:name=".principal.ActivityCuentasDetalle" />
        <activity android:name=".principal.CuentasActivity" />
        <activity
            android:name=".principal.TransferenciasActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".principal.ClaveActivity" />
        <activity android:name=".principal.PrincipalActivity" />
        <activity android:name=".principal.LoginActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login"
            tools:ignore="MissingClass" />
        <activity android:name=".principal.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>